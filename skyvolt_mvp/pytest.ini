[pytest]
# Two packages each have a `test/` directory with the same name; importlib
# import mode keeps them distinct.
addopts = --import-mode=importlib
testpaths =
    src/skyvolt_localization/test
    src/skyvolt_fleet/test
